<h2>Editar categoria</h2>

<form action="/frontController/editarCategoriaProcess" method="post">
    <input type="hidden" name="id" value="{{categoria._id}}">
    <input type="text" name="nome" placeholder="Nome da categoria"  value="{{categoria.nome}}" required><br>
    <input type="text" name="link" placeholder="Link da categoria" value="{{categoria.link}}" required><br>
    <button type="submit">Confirmar edição</button>
</form>